cc_library(
    name = "flex_lib",
    srcs = glob(["src/*.c", "src/*.h"], exclude = ["src/lib*.c"]),
    features = ["no_copts_tokenization"],
    copts = [
        "-DHAVE_ASSERT_H",
        "-DHAVE_LIMITS_H",
        '-DVERSION="{version}"',
        '-DM4="/bin/false"',
    ],
    visibility = ["//bin:__pkg__"],
)
